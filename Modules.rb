#Modules are way to group varibles, methods and classes together in names space
# the difference between Module and Class, is Module can't be instantiated


module WordProcessor
  def self.points_to_inches(points)
    #its important to call the self.method - otherwise the method is available globally
  end
end
